
t=$(loginctl show-session $(loginctl | awk '/tty/ {print $1}') -p Type | awk -F= '{print $2}')

echo =${t}
a=${1}
shift
b=${1}
shift
if [ "${t}" = "x11" ]; then
	if [ "${b}" = "enter" ]; then b="Return"; fi
	sudo xdotool ${a} ${b} $@
else
	sudo ydotool ${a} ${b} $@
fi
