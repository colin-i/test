
#1 H:MM or M

if [ -n "`echo -n ${1} | grep :`" ]; then
	a=$((`date -d ${1} +%s`-`date +%s`))
else
	a=$((${1}*60))
fi
pause () {
	echo ${1}
	sleep ${1}
}
xrandr --output eDP-1 --off
pause ${a}
xrandr --output eDP-1 --auto
pause 60
~/test/keyring
~/test/net/mm/ns.sh
