
#id=`ps -C sh -o pid,cmd | grep "test/net/head/z" | grep -o "^ *[^ ]*" | grep -o "[^ ]*$"` #a few times with leading space, a few times without
id=`ps -C sh -o pid,cmd | grep "test/net/head/z" | grep -o "^[^ ]*"` #only without?
if [ -n "${id}" ]; then
	kill -s 9 ${id}
fi
