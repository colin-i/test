
source ~/test/startup/bashrc

f=~/plug_serv
last=$(date -d @$(stat -c %Y ${f}) +%d)
today=`date +%d`
if [ "${last}" != "${today}" ]; then
	systemctl status plug-temperature-dog.service
	tail ~/_temp.log
	touch ${f}
fi
