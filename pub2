
#optional 1 name

if [ -z "${1}" ]; then
url=`cat debian/control | grep -Poe "(?<=^Homepage: ).*"`
else
url="https://github.com/colin-i/${1}"
fi
url+="/releases/latest"

echo open browser
#firefox ${url}
notify-send "Publish" "${url}"
