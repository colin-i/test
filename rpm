
#optional:    1 ppa sufix    2 unpublished

proj=`cat debian/changelog | head -1 | grep -o ^[^\ ]*`
if [ "${proj}" = "ocompiler" ] || [ "${proj}" = "ovideo" ]; then
	gh workflow run rpm.yml -f ppa=${1} -f unpublished=${2}
else
	gh workflow run rpm.yml -f ppa=${1}
fi
