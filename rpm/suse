
if [ -e ./cln ]; then
	echo cln. enter
	read -t $((60*60*24))
	./cln
fi

~/test/rpm/pt || exit 1

if [ -n "${py}" ]; then py="py=313 "; fi
echo "${py}~/test/rpm/osc"
echo -n "${py}~/test/rpm/osc" | xclip -sel clip
echo "ready? enter"
read -t $((60*60*24))

#313 is ephemeral, usually is last Leap version, and is for install after tests, see them at https://download.opensuse.org/repositories/home:/costin/
