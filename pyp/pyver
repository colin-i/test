
echo pypi.org \? y enter && \
read -t $((60*60*24)) ans && \
if [ "${ans}" = "y" ]; then
	~/test/xver ./v2
else
	if [ -z "${exit}" ]; then
		exit 1
	else
		exit ${exit}
	fi
fi

# https://pypi.org/pypi/audapa/json
