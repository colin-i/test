
echo pypi.org \? y \? && \
read -n1 -t $((60*60*24)) ans && \
if [ "${ans}" = "y" ]; then
#version at v2 is incremented at ./pyver (then pushed)
	~/test/pyp/pyin && \
	~/test/pyp/pyout && \
	~/test/pyp/pyin x && \
	~/test/pyp/pyqu
	# && yank
else
	echo no
fi
