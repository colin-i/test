
#1 year  2 month

./prepare ${1} ${2} && \
echo -e "\n\npush?" && \
read -n1 -t $((60*60*24)) ans && \
if [ "${ans}" = "y" ]; then
./com ${1} ${2}
else
echo no
fi

#when adding new project:
#	add at h: proj=X/0
#	prepare
#	if X, add it at y/m/h (for reconcatenator)
#	push
