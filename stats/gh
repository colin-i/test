
#1 folder

cd ${1}

ins=( `cat h3` )

i=0

declare -A arr

for var in "${ins[@]}"
do
	IFS='=' read -a addr <<< "${var}"
	arr[$i,0]=${addr[0]}
	arr[$i,1]=${addr[1]}
	i=$((i+1))
done

for ((j=0;j<i;j++)) do
	echo ${arr[$j,1]} ${arr[$j,0]} >> 1
done

sort -k1nr 1
