

rps=( `cat g` )
i=0

QUERY='
{"query": "
  query {
    viewer {
      repositories(first:100){
        edges{
          node{
            name
          }
        }
      }
    }
  }"
}'

#-o 1
reps=( $(
curl https://api.github.com/graphql -X POST -H "Authorization: bearer `cat ~/n/pat`" --data "$(echo $QUERY)" | \
  jq -r ".data.viewer.repositories.edges[] | .node.name"
))

for name in "${reps[@]}"; do

./g1 ${name} `echo -n ${rps[${i}]} | grep -o [^=]*$`

i=$((i+1))

done
