
cd `dirname ${0}`
dir=`pwd`
cd ~/gree-remote/PythonCLI

if [ -z "${1}" ]; then
sen2=0
else
sen2=1
fi

if [ -z "${2}" ]; then
dif=0
else
dif=${2}
fi

if [ -z "${3}" ]; then
min=25
else
min=${3}
fi

if [ -z "${4}" ]; then
max=27
else
max=${4}
fi

if [ -z "${5}" ]; then
send=1
else
send=0
fi

if [ -z "${6}" ]; then
base=${dir}
else
base=${6}
fi

python ${dir}/temperature_sensor_code.py ${sen2} ${dif} ${min} ${max} ${send} ${base}
