#/bin/bash

shopt -s expand_aliases
source ~/test/misc/aliases

files=( `gre ${1} -l | xargs` )

for var in "${files[@]}"
do
sed -i "s/${1}/${2}/g" ${var}
done

gre ${2}

#find ./ -type f -exec sed -i 's/A/a/g' {} \;
