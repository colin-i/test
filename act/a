
#launch command: ~/test/act/a &
resize -s 36 132

while :
do
	a=`timedatectl -p NTPSynchronized show`
	echo ${a}
	if [ "${a}" = "NTPSynchronized=yes" ]; then break; fi
	#even at init 6
	sleep 10
done

~/arh/activity
