
cp ~/a/tmp/ffdec/translation.jpt ~/.config/FFDec/translated.zip # because it is autosaved on close. want diff 0 and timestamps inside file differs
sync # because cp is in script and ffdec doing errors sometimes
cd ~/ffdec/bin
gsettings set org.gnome.desktop.interface gtk-theme Yaru
./ffdec -translator
gsettings set org.gnome.desktop.interface gtk-theme Yaru-dark
